mutation insertReview($review: ReviewInputType!) {
  insertReview(review: $review) {
    id
    is_mine
    course_id
    semester_id
    difficulty
    workload
    rating
    body
    created
    updated
  }
}
