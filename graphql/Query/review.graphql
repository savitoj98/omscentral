query review($id: String!) {
  review(id: $id) {
    id
    author_id
    course {
      id
      name
    }
    semester {
      id
      name
      season
    }
    difficulty
    rating
    workload
    body
    created
    updated
  }
}
